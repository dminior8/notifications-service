spring:
  application:
    name: producer-service

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  data:
    mongodb:
      uri: mongodb://localhost:27017/notifications-db
      database: notifications-db
